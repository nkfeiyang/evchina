2010
   10-29:
    1.事件的增加修改页面，实现了对活动参与者的信息收集的管理。
   10-28:
    1.票务信息，直接在events表增加一个字段。price。
    2.事件管理页面，增加了票价输入字段。增加了需要收集用户信息的控制面板
    3.增加表ticket_orders,保存用户报名参加活动的数据
    4.增加表ticket_order_details,保存用户报名时提交的注册信息
    5.增加表event_reg_requires,记录一个事件需要收集的用户信息选项
   10-27:
         用户个人控制面板中，删除收藏事件
         关于事件发布者的信息，将其转移到了event模型中去了。原有的host_info模型暂不使用。
         增加了事件的hostinfo字段，以及页面相应的添加修改
         增加了事件的状态编辑。
         事件显示时，对状态做了判断，凡是不是published的事件都不能显示。这个应该不完整，比如某个用户被禁止时，它的事件应该都停止。
        增加表:sys_reg_infos，提供可选的用户报名时收集信息项                                       
   10-26:
         在事件列表页面添加对事件的收藏和取消按钮，功能已完成。另添加对发布者的收藏，功能没有完成。